// arrow functions better, DoSomething = () => {}

// if age greater than 10, set name to pedro else jack
// let name = age > 10 ? "Predro" : "Jack"

const person = {
    name:"Pedro",
    age:20,
    isMarried: false,
};

const {name, age, isMarried} = person;